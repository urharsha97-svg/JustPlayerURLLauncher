pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)

    repositories {
        google()
        mavenCentral()
    }
}

// Extract only the Android modules from the payload ZIP.
// Do NOT extract the payload's own root build/settings files.
def payload = new File(settingsDir, "streamlink_payload.zip")
def marker = new File(settingsDir, ".streamlink_payload_extracted")

if (payload.exists() && !marker.exists()) {
    payload.withInputStream { input ->
        new java.util.zip.ZipInputStream(input).withCloseable { zis ->
            def entry

            while ((entry = zis.nextEntry) != null) {
                def entryName = entry.name.replace("\\", "/")

                // Only extract these folders.
                if (!(entryName.startsWith("tv/") ||
                      entryName.startsWith("sender/") ||
                      entryName.startsWith("relay/"))) {
                   
